<<<<<<< Updated upstream
DATABASE_URL="file:./dev.db"
=======
# Local development (PostgreSQL)
# Install PostgreSQL locally and create a database named "otkaz_dev"
DATABASE_URL="postgresql://postgres:password@localhost:5432/otkaz_dev?schema=public"

# Production (Vercel)
# Vercel will automatically inject these variables when using Vercel Postgres:
# - POSTGRES_PRISMA_URL (pooled connection for queries)
# - POSTGRES_URL_NON_POOLING (direct connection for migrations)
# The app will use them automatically (see lib/prisma.ts)

# Authentication
>>>>>>> Stashed changes
NEXTAUTH_SECRET="change-this-to-a-random-secret"
NEXTAUTH_URL="http://localhost:3000"